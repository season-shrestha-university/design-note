---
import { getCollection } from "astro:content";
import { deriveCategory, formatCategoryLabel } from "../utils";

type ActiveSection = string | "home" | "none";

interface Props {
  activeSection?: ActiveSection;
}

const { activeSection = "none" } = Astro.props;

const articles = await getCollection("articles");
const categories = [
  ...new Set(articles.map((article) => deriveCategory(article.id))),
]
  .sort()
  .reverse();
---

<!-- <input type="checkbox" id="nav-toggle" /> --><!-- <nav>
  <label for="nav-toggle">
    <div>
      <span></span>
      <span></span>
      <span></span>
    </div>
  </label>
</nav> -->
<aside>
  <!-- <label for="nav-toggle" aria-label="Close sidebar menu">×</label>
  <nav>
    {
      categories.map((category, index) => (
        <a href={`/categories/${category}/`}>
          <div>{categories.length - index}</div>
          <div>
            <span>{formatCategoryLabel(category)}</span>
            {activeSection === category && <div>CURRENT</div>}
          </div>
        </a>
      ))
    }
    <a href="/">
      <div>0</div>
      <div>
        <span>Introduction</span>
        {activeSection === "home" && <div>CURRENT</div>}
      </div>
    </a>
  </nav> -->

  <a href="/">
    <svg
      class="back__btn"
      xmlns="http://www.w3.org/2000/svg"
      width="32"
      height="32"
      fill="none"
      viewBox="0 0 32 32"
      ><path fill="currentColor" d="M19 2 7 16l12 15 4-3-11-12L23 4z"
      ></path></svg
    >
  </a>
</aside>
