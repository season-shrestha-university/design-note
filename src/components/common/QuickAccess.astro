---
import { ARTICLE_CATEGORIES, formatCategoryLabel } from "@/utils";

const pathname = Astro.url.pathname.replace(/\/$/, "") || "/";

const ariaCurrent = (slug?: string) => {
  if (!slug) return pathname === "/" ? "page" : undefined;
  return pathname === `/categories/${slug}` ||
    pathname.startsWith(`/articles/${slug}/`)
    ? "page"
    : undefined;
};

const categoryIcons: Record<string, string> = {
  "user-research":
    '<rect x="2" y="2" width="116" height="116" stroke="currentColor" stroke-width="4" data-fill="primary"/><rect x="27.5" y="27.5" width="60" height="60" stroke="currentColor" stroke-width="4" data-fill="accent"/>',
  "user-experience":
    '<circle cx="60" cy="60" r="58" stroke="currentColor" stroke-width="4" data-fill="primary"/><circle cx="60.2712" cy="60.2717" r="35.8371" stroke="currentColor" stroke-width="4" data-fill="accent"/>',
  "user-interface":
    '<circle cx="60" cy="60" r="58" stroke="currentColor" stroke-width="4" data-fill="accent"/>',
  "interaction-design":
    '<path d="M83.5395 29.3113L67.2736 100.729L17.5542 49.96L83.5395 29.3113Z" stroke="currentColor" stroke-width="4" stroke-linejoin="round" data-fill="primary"/><ellipse cx="94.2209" cy="27.562" rx="25.779" ry="27.56" stroke="currentColor" stroke-width="4" data-fill="accent"/>',
  "usability-testing":
    '<rect x="2" y="2" width="48" height="116" rx="8" stroke="currentColor" stroke-width="4" data-fill="accent"/><rect x="70" y="2" width="48" height="116" rx="8" stroke="currentColor" stroke-width="4" data-fill="primary"/>',
};
---

<nav class="sidebar" aria-label="Site navigation">
  <ul class="sidebar__list">
    <li class="sidebar__item">
      <a href="/" aria-label="Home" aria-current={ariaCurrent()}>
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="32"
          height="32"
          viewBox="0 0 512 512"
          fill="none"
        >
          <rect
            x="460"
            y="52"
            width="407"
            height="407"
            transform="rotate(90 460 52)"
            stroke="#979D8A"
            stroke-width="12"></rect>
          <rect
            x="256.536"
            y="91.5006"
            width="231.738"
            height="231.97"
            transform="rotate(45.0028 256.536 91.5006)"
            stroke="#979D8A"
            stroke-width="12"></rect>
          <rect
            x="324.714"
            y="179.256"
            width="144.64"
            height="144.64"
            rx="72.3199"
            transform="rotate(87.1271 324.714 179.256)"
            fill="#979D8A"></rect>
        </svg>
      </a>
    </li>
    {
      ARTICLE_CATEGORIES.map(({ slug }) => (
        <li class="sidebar__item">
          <a
            href={`/categories/${slug}/`}
            class="sidebar__item-link"
            aria-current={ariaCurrent(slug)}
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="28"
              height="28"
              viewBox="0 0 120 120"
              fill="none"
              aria-hidden="true"
              set:html={categoryIcons[slug]}
            />
            <span class="sidebar__item-title">
              {formatCategoryLabel(slug)
                .split(" ")
                .map((line) => (
                  <span>{line}</span>
                ))}
            </span>
          </a>
        </li>
      ))
    }
  </ul>
</nav>
